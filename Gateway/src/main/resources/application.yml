server:
  port: 8888
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
          globalcors:
            cors-configurations:
              '[/**]':
                allowed-origins: ["http://34.171.39.26:3000", "http://gamey.gcp:3000"]
                allowed-methods:
                  - GET
                  - POST
                  - PUT
                  - DELETE
                  - OPTIONS
                allowed-headers:
                  - "*"
                allow-credentials: true
          routes:
            - id: discovery
              uri: lb://WORKER.DISCOVERY
              predicates:
                - Path=/discovery/**
              filters:
                - StripPrefix=1
            - id: details
              uri: lb://WORKER.DETAIL
              predicates:
                - Path=/details/**
              filters:
                - StripPrefix=1
            - id: aggregator
              uri: lb://AGGREGATOR
              predicates:
                - Path=/aggregator/**
              filters:
                - StripPrefix=1
  data:
    mongodb:
      uri: mongodb://root:root@mongodb:27017/gamey?authSource=admin
  kafka:
    bootstrap-servers: PLAINTEXT://kafka:9092

logging:
  level:
    web: trace
    reactor.netty.http.client: info
    reactor.netty.http.server: info

diffbot:
  api:
    token:
      d0984ff1efff1a0824ae40bf4632ff25
eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/
